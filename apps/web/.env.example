# API base URL (no trailing slash)
# Local API (desarrollo y pruebas — recomendado):
VITE_API_URL=http://localhost:3334
# Producción Railway (solo al distribuir; no usar en dev diario):
# VITE_API_URL=https://moda-urbana-production.up.railway.app
